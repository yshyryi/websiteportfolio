{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "powershell -NoProfile -Command \"Set-Location 'C:\\Work\\Website'; $changes = git status --porcelain; if ($changes) { git add -A; git commit -m 'Auto-update: portfolio changes'; git push }\"",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
