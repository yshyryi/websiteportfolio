# Large source/archive files — not needed for the deployed site
*.zip
*.rar
*.7z

# 3D source files (важкі, не потрібні для сайту)
*.blend
*.blend1
*.blend2
*.spp
*.max
*.fbx
*.obj
*.gltf
# GLB дозволені — потрібні для model-viewer на сайті
# (якщо треба виключити конкретний файл: !НазваФайлу.glb)

# OS
.DS_Store
Thumbs.db
desktop.ini

# Локальні налаштування Claude Code (особисті, не для репозиторію)
.claude/settings.local.json
